/** BYIMAAN */

class Heap {
    protected storage:number[] = []

    constructor(){
    }
    
    protected node = class Node {
        constructor(public index:number){
            // if (index < 0 || index >= this.storage)
            this.index = index
        };

        left(){
            
        }
    }
}